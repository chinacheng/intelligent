=stylesheet_link_tag 'index'

.top_area
  .pictures
  .top_news
    %ul
      -@articles.each do |article|
        %li
          =link_to article.name, article_path(article),:class=>"data" 
.area
  .left_area
  .main_area
    .middle_container
      .commend
      .news
    .right_container

